# satzstrom

Platform-selecting launcher for the Satzstrom CLI.

```sh
npm install -g satzstrom
satzstrom --help
```

The launcher is MIT licensed. It installs one proprietary native renderer
package selected by npm for the current operating system and CPU architecture.
